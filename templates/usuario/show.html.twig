{% extends 'base.html.twig' %}

{% block title %}Usuario{% endblock %}

{% block body %}
    <h1>Usuario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ usuario.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ usuario.nombre }}</td>
            </tr>
        </tbody>
    </table>
<div class="d-flex justify-content-end gap-2">
    <a href="{{ path('app_usuario_index') }}" class="btn btn-success">Volver a la lista</a>

    <a href="{{ path('app_usuario_edit', {'id': usuario.id}) }}" class="btn btn-success">Editar</a>
</div>
    {{ include('usuario/_delete_form.html.twig') }}
{% endblock %}
